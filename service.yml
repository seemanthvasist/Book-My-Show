apiVersion: v1
kind: Service 
metadata:
  name: bms-service
  namespace: vasist-ns
spec:
  selector:
    app: bms-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000 # container port, it should be same as container port defined in deployment.yaml
  type: LoadBalancer
